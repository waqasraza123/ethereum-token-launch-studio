import type { Metadata } from "next";

export const siteMetadata: Metadata = {
  title: {
    default: "Ethereum Token Launch Studio",
    template: "%s | Ethereum Token Launch Studio",
  },
  description:
    "A reusable Ethereum token operations platform for token launches, claims, vesting, treasury workflows, and operator tooling.",
};
